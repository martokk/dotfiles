#!/bin/bash

# -------------------------------------------------------------------------
# SSH INTO SERVERS
# -------------------------------------------------------------------------
alias ssh_aws="ssh -i /media/martokk/FILES/Servers/aws-server/.credentials/aws-server.pem ubuntu@aws-server"
alias ssh_vserver="ssh vserver@vserver"

# -------------------------------------------------------------------------
# START VM'S
# -------------------------------------------------------------------------
alias start_vserver="sh ~/Scripts/Shell/start_vserver_in_vm.sh"

# -------------------------------------------------------------------------
# UNISON SERVER SYNC
# -------------------------------------------------------------------------
alias sync_aws="unison aws-server"
